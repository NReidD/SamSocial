<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="/styles/navbar.css">
    <title>Edit Organization</title>
</head>
<body>
    <%- include('../partials/navbar') %>

    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8 col-lg-6">
                <h1 class="text-center mb-4">Edit Organization</h1>
                <form action="/organizations/<%= org._id %>/edit?_method=PUT" method="POST" class="p-4 border rounded-3 shadow-sm bg-light">
                    <div class="mb-3">
                        <label for="name" class="form-label">Organization Name</label>
                        <input type="text" class="form-control" name="Name" id="name" value="<%= org.Name %>" required autocomplete="off">
                    </div>

                    <div class="mb-3">
                        <label for="category" class="form-label">Category</label>
                        <input type="text" class="form-control" name="Category" id="category" value="<%= org.Category %>" required autocomplete="off">
                    </div>

                    <div class="mb-3">
                        <label for="bio" class="form-label">Bio</label>
                        <textarea class="form-control" name="Bio" id="bio" rows="3" required autocomplete="off"><%= org.Bio %></textarea>
                    </div>

                    <div class="mb-3">
                        <label for="founded" class="form-label">Founded</label>
                        <input type="date" class="form-control" name="Founded" id="founded" value="<%= org.Founded %>" required autocomplete="off">
                    </div>

                    <div class="mb-3">
                        <label for="profilepic" class="form-label">Profile Picture URL</label>
                        <input type="url" class="form-control" name="ProfilePicture" id="profilepic" value="<%= org.ProfilePicture %>" required autocomplete="off">
                    </div>

                    <button type="submit" class="btn btn-primary w-100">Update Organization</button>
                </form>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.min.js" integrity="sha384-BBtl+eGJRgqQAUMxJ7pMwbEyER4l1g+O15P+16Ep7Q9Q+zqX6gSbd85u4mG4QzX+" crossorigin="anonymous"></script>
</body>
</html>
