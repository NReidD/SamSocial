<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Organizations Index</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/styles/navbar.css">
    <link rel="stylesheet" href="/styles/style.css">
</head>
<body>
    <%- include('../partials/navbar') %>

    <div class="container mt-5">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1 class="text-center">Organizations</h1>
            <a href="/organizations/new" class="btn btn-pink">New Organization</a>
        </div>

        <% if (orgs.length > 0) { %>
            <div class="row g-4">
                <% for (let i = 0; i < orgs.length; i++) { %>
                    <div class="col-md-6 col-lg-4">
                        <div class="card shadow-sm h-100">
                            <img src="<%= orgs[i].ProfilePicture %>" class="card-img-top" style="height: 200px; object-fit: contain;" alt="<%= orgs[i].Name %>">
                            <div class="card-body">
                                <h5 class="card-title"><%= orgs[i].Name %></h5>
                                <p class="card-text text-truncate"><%= orgs[i].Bio %></p>
                                <a href="/organizations/<%= orgs[i]._id %>" class="btn btn-pink">Show More</a>
                            </div>
                        </div>
                    </div>
                <% } %>
            </div>
        <% } else { %>
            <p class="text-center text-muted">No organizations available.</p>
        <% } %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.min.js" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous"></script>
</body>
</html>
