<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <title>Show Post</title>
    <link rel="stylesheet" href="/styles/navbar.css">
</head>
<body>
    <%- include('../partials/navbar') %>

    <div class="container mt-5">
        <a href="/organizations/<%= org._id %>" class="btn btn-secondary mb-4">Back to Organization</a>

        <div class="card shadow-sm">
            <div class="card-body">
                <h1 class="card-title text-center"><%= post.Title %></h1>
                <div class="text-center my-4">
                    <img src="<%= post.Media %>" alt="Post Media" class="img-fluid rounded">
                </div>
                <p class="card-text"><strong>Caption:</strong> <%= post.Caption %></p>
                <p class="card-text text-muted"><strong>Created:</strong> <%= post.Created %></p>
            </div>

            <% if (currentUser && org.Admin.equals(currentUser._id)) { %>
                <div class="card-footer d-flex justify-content-between">
                    <a href="/organizations/<%= org._id %>/posts/<%= post._id %>/edit" class="btn btn-warning">Edit</a>
                    <form action="/organizations/<%= org._id %>/posts/<%= post._id %>?_method=DELETE" method="POST" class="d-inline">
                        <button class="btn btn-danger">Delete</button>
                    </form>
                </div>
            <% } %>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.min.js" integrity="sha384-BBtl+eGJRgqQAUMxJ7pMwbEyER4l1g+O15P+16Ep7Q9Q+zqX6gSbd85u4mG4QzX+" crossorigin="anonymous"></script>
</body>
</html>
