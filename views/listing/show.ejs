<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Listing</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="/styles/navbar.css">
</head>
<body>
    <%- include('../partials/navbar') %>

    <div class="container mt-5">
        <a href="/organizations/<%= orgId %>" class="btn btn-secondary mb-4">Back to Organization</a>

        <div class="card shadow-sm">
            <div class="card-body">
                <h1 class="card-title text-center mb-4"><%= listing.EventName %></h1>
                <p class="card-text"><strong>Category:</strong> <%= listing.Category %></p>
                <p class="card-text"><strong>Date:</strong> <%= listing.Date %></p>
                <p class="card-text"><strong>Start Time:</strong> <%= listing.Start %></p>
                <p class="card-text"><strong>End Time:</strong> <%= listing.End %></p>
                <p class="card-text"><strong>Description:</strong> <%= listing.Description %></p>
            </div>

            <% if (currentUser && org.Admin.equals(currentUser._id)) { %>
                <div class="card-footer d-flex justify-content-between">
                    <a href="/organizations/<%= orgId %>/listings/<%= listing._id %>/edit" class="btn btn-warning">Edit</a>
                    <form action="/organizations/<%= orgId %>/listings/<%= listing._id %>?_method=DELETE" method="POST" class="d-inline">
                        <button class="btn btn-danger">Delete</button>
                    </form>
                </div>
            <% } %>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.min.js" integrity="sha384-BBtl+eGJRgqQAUMxJ7pMwbEyER4l1g+O15P+16Ep7Q9Q+zqX6gSbd85u4mG4QzX+" crossorigin="anonymous"></script>
</body>
</html>
